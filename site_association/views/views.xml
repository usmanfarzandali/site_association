<odoo>
  
    <!-- inherit id -->
    <record model="ir.ui.view" id="contacts_inherit">
        <field name="name">contacts.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
          <field name="vat" position="after">
            <field name="avg_no_of_employees"/>
          </field>
          <field name="avg_no_of_employees" position="after">
            <field name="date_of_establishment"/>
          </field>
        </field>  
    </record>

</odoo>